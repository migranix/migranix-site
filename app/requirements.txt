# ============================================================
# Migranix Backend — All 14 database drivers
# For Hetzner CX22+ (2vCPU / 4GB RAM)
# ============================================================

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-multipart==0.0.9
httpx==0.27.0

# ---- RELATIONAL DATABASES ----
psycopg2-binary==2.9.9          # PostgreSQL + Redshift
PyMySQL==1.1.1                   # MySQL / MariaDB
pyodbc==5.1.0                    # SQL Server (needs ODBC Driver in Dockerfile)
python-oracledb==2.1.0           # Oracle — thin mode, no Instant Client needed!
SQLAlchemy==2.0.30               # ORM / query layer for all relational DBs
# ibm-db and ibm-db-sa not included — IBM DB2 needs proprietary IBM client libs
# that are not freely distributable; show a clear error when attempted

# ---- CLOUD WAREHOUSES ----
snowflake-connector-python==3.10.0
databricks-sql-connector==3.1.0
google-cloud-bigquery==3.25.0
google-auth==2.29.0
google-cloud-storage==2.17.0

# ---- NOSQL ----
pymongo==4.7.2
cassandra-driver==3.29.1

# ---- CLOUD STORAGE & EVENTS ----
boto3==1.34.129                  # AWS: DynamoDB + S3
azure-storage-blob==12.20.0      # Azure Blob Storage
azure-cosmos==4.7.0              # Azure Cosmos DB

# ---- ENCRYPTION (Snowflake key-pair auth) ----
cryptography==42.0.7

# ---- DATA PROCESSING ----
pandas==2.2.2
openpyxl==3.1.2
pyarrow==16.0.0
lxml==5.2.2
fastavro==1.9.4
